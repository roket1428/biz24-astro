---
import { Image } from "astro:assets";

interface Props {
    src: ImageMetadata;
    alt: string;
}

const { src, alt } = Astro.props;
---

<Image class="m-5 mb-12 min-h-52 max-h-60 object-contain w-[320px]" src={src} alt={alt} />
